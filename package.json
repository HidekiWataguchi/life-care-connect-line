{
  "name": "life-care-connect",
  "version": "0.2.0",
  "private": true,
  "description": "LINE official account safety check prototype for care operations.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js && node --check netlify/functions/api.js"
  },
  "engines": {
    "node": ">=18"
  }
}
